version: 2.1

executors:
  worker:
    docker:
      - image: fr3akyphantom/droid-builder:bionic

jobs:
  build:
    executor: worker
    steps:
      - run:
          name: Start Baking!
          command: |
              echo " HEllO World "
              ls
              df -h
              mkdir sam
              ls
              sudo apt update && apt upgrade -y
              sudo apt install neofetch
              neofetch
